<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:health="clr-namespace:Health" 
             x:DataType="health:ClockViewModel"
             xmlns:local="clr-namespace:Health.Views"
             xmlns:sys="clr-namespace:System;assembly=netstandard"
             x:Class="Health.Views.SchedulerPage">
    <ContentPage.BindingContext>
        <health:ClockViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <x:Double x:Key="dbl">
            50
        </x:Double>
    </ContentPage.Resources>
    <StackLayout>
        <Label Text="{Binding DT}" FontSize="Large" HorizontalOptions="Center"></Label>
    </StackLayout>
</ContentPage>
    